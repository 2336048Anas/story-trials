{
  "name": "story-trials",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "prisma": "6.15.0"
  },
  "dependencies": {
    "@prisma/client": "6.15.0"
  }
}
